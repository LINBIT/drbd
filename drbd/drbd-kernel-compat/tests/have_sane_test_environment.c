/* Check if our test cases are test-compiled against a sane environment.
 * For now, just include some header files we expect to be always present.
 */
#include <linux/module.h>
#include <linux/moduleparam.h>
